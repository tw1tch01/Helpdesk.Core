namespace Helpdesk.Domain.Tickets.Enums
{
    public enum TicketStatus
    {
        Open = 0,
        Overdue = 1,
        Resolved = 2,
        Closed = 3,
        InProgress = 4,
        OnHold = 5
    }
}